extends layout

block content
  h1= title

   
  div(class="container-fluid")
   div(class="row")
    - let results=[{gift_name:"Diamond",numof_gifts:1,gift_value:"Most Valuable"},{gift_name:"Gold",numof_gifts:2,gift_value:"More Valuable"},{gift_name:"Car",numof_gifts:1,gift_value:"Valuable"}]
      each value,index in results
       div(class="col-sm-2" onclick=`buy(${index})`)  #{value.gift_name} Gift, Number of Gifts is/are #{value.numof_gifts}, and it is #{value.gift_value}
  script. 
    function buy(index)
    {
      alert(`You purchased Item ${index+1}`)
    }
  